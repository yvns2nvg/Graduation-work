# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0      # SQLite 비동기 드라이버 (로컬 개발용)
greenlet>=3.0.0        # 비동기 SQLAlchemy 필수 의존성
asyncpg>=0.30.0        # PostgreSQL 비동기 드라이버 (Cloud Run 배포용)

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# HTTP Client (외부 서버 통신)
httpx>=0.28.0

# Supabase (Database & Storage)
supabase>=2.4.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.0
pydantic-settings>=2.7.0
email-validator>=2.2.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.25.0
