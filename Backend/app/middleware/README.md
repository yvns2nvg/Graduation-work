# 🛡️ Middleware (중간 검문소)

이 폴더는 클라이언트의 API 요청이 실제 라우터(`routers/`)에 도착하기 전, 혹은 빠져나갈 때 공통적으로 거치며 **검사나 처리**를 진행하는 코드들을 모아둔 곳입니다.

## 주요 파일 목록
* `auth.py`: 사용자가 API를 요청할 때 함께 보낸 JWT 토큰이 조작되지 않았고 유효시간이 남았는지 확인하여, "로그인된 올바른 사용자"인지 판단하는 핵심 인증(Authentication) 미들웨어 역할
